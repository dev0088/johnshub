import { put, takeLatest, call } from 'redux-saga/effects';
// import * as types from '../actions/types';
// import { processRequest } from '../services/api';
// import serverUrls from '../config/api';
// import { UserActions } from '../actions';


export default function* watcher() {
  // yield takeLatest(types.GET_USERS.REQUEST, getUsers);
}

export function* getUsers(action) {
  // try {
  //   // Get average duration.
  //   const response = yield call(
  //     processRequest,
  //     `${serverUrls.apiGatewayServerURL}/user/all`,
  //     'GET',
  //     null
  //   );
  //   const data = response.data;
  //   yield put(UserActions.getUsersSuccess(data));
  // } catch(error) {
  //   yield put(UserActions.getUsersFailed(error))
  // }
}